export function metersToMiles(meters: number) {
    return (Math.round(meters) / 1609).toFixed(2)
}

export function milesToMeters(miles: number) {
    return (Math.round(miles) * 1609).toFixed(2)
}

//fdsfsfsfsfsfsfs
//fsfsfsfsfsfsfsf